Dim angka As Integer 'Nomor 1'
Input "Masukkan angka : "; angka

If angka > 0 Then
    Print "Ini adalah bilangan positif"
ElseIf angka < 0 Then
    Print "Ini adalah bilangan negatif"
Else
    Print "Ini adalah bilangan nol"
End If
